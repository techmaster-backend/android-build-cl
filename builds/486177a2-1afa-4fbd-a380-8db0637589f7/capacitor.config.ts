
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  "appId": "com.karbonsites.minigame",
  "appName": "Mini Game",
  "webDir": "www",
  "server": {
    "androidScheme": "https"
  }
};

export default config;
    